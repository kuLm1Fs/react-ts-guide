# TSX 文件常见错误总结

## 1. 错误：Expected ';', '}' or <eof>
**原因**：文件内容不是 JSX 格式
**场景**：写了纯 markdown 文本或普通文本
**解决**：文件必须是 React 组件，导出 default function

## 2. 错误：Expected '</', got 'string literal'
**原因**：JSX 中 `{}` 嵌套冲突
**场景**：
- `{{ name: '张三' }}` - 第一个 `{` 被解析成对象开始，第二个 `{` 被解析成表达式
- `{name}` 在 text 节点中会被当成变量
**解决**：
- 对象用 `{'{'}"name"` 表示 `{`
- 文本中的变量用 `&#123;name&#125;` 或 `{'{'}{name}{'}'}`
- 尽量避免在文本中混合 `{}`

## 3. 错误：Expected a semicolon
**原因**：JSX 中双花括号 `{{` 会被解析成对象字面量
**场景**：`code={`{ name: 'test' }`}`
**解决**：避免在 JSX 属性中使用对象字面量，或用 `{'{'}` 转义

## 4. 错误：Expected unicode escape
**原因**：JSX 属性中的 `expectedOutput="...[\"...]` 包含未转义的引号
**场景**：双引号字符串属性中有 JSON 数组输出，双引号在 JSX 中被错误解析
**解决**：用模板字符串 `` ` `` 包裹，或把双引号改成单引号

```
// 错误 ❌
expectedOutput="包含果: [\"苹果\",\"草莓\"]"

// 正确 ✅
expectedOutput={`包含果: ["苹果","草莓"]`}
```
**原因**：JSX 中 `{role}` 被当成 JS 表达式求值
**场景**：字符串中包含 `{variable}`
**解决**：使用 HTML 实体 `&#123;` 和 `&#125;` 或 `{'{'}` 转义

---

## 预防措施

1. **TSX 文件必须是有效的 JSX 组件**
   - 必须有 `"use client"` 指令（如果是 client component）
   - 必须导出 default function
   - 所有文本必须放在 JSX 标签内

2. **避免在文本中直接使用 `{}`**
   - 花括号在 JSX 中是表达式语法
   - 如需显示花括号，用 `{'{'}` 或 HTML 实体

3. **代码块用 `<pre>` 标签**
   - 代码不要放在 JSX 属性的字符串中
   - 用 `children` 方式传入

4. **对象字面量小心处理**
   - 在 JSX 属性中避免 `{key: value}` 形式
   - 用模板字符串或单独的变量

---

## 快速检查清单

- [ ] 文件顶部的 `#` 语法会报错（不是 markdown）
- [ ] `{{` 或 `}}` 在 JSX 中会报错
- [ ] `{variable}` 在文本中会被当成表达式
- [ ] 普通文本不在标签内会报错
- [ ] 所有代码示例用 `<pre>` 或 template literal